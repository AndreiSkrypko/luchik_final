# Инструкция по деплою проекта через GitHub

## 🚀 Автоматический деплой при пуше в GitHub

Проект настроен для автоматического деплоя при каждом `git push` в репозиторий!

---

## Frontend (Next.js) на Vercel

### Шаг 1: Подключение Vercel к GitHub

1. **Перейдите на [vercel.com](https://vercel.com)** и войдите через GitHub
2. **Нажмите "Add New Project"**
3. **Выберите репозиторий** `AndreiSkrypko/luchik_final`
4. **Настройте проект:**
   - **Root Directory:** `frontend` (важно!)
   - Framework Preset: Next.js (определится автоматически)
   - Build Command: `npm run build` (по умолчанию)
   - Output Directory: `.next` (по умолчанию)
5. **Нажмите "Deploy"**

### Шаг 2: Автоматический деплой

✅ После первого деплоя Vercel автоматически будет деплоить проект при каждом `git push` в ветку `main`!

**Ваш сайт будет доступен по адресу:** `https://luchik-final.vercel.app` (или ваш кастомный домен)

---

## Backend (Django) на Railway

### Шаг 1: Подключение Railway к GitHub

1. **Перейдите на [railway.app](https://railway.app)** и войдите через GitHub
2. **Нажмите "New Project" → "Deploy from GitHub repo"**
3. **Выберите репозиторий** `AndreiSkrypko/luchik_final`
4. **Настройте проект:**
   - **Root Directory:** `backend` (важно!)
   - Build Command: автоматически определится из `railway.json`
   - Start Command: автоматически определится из `railway.json`

### Шаг 2: Настройка переменных окружения

В настройках проекта Railway добавьте переменные окружения:

```
SECRET_KEY=ваш-секретный-ключ-здесь
DEBUG=False
ALLOWED_HOSTS=*.railway.app,your-domain.com
CORS_ALLOWED_ORIGINS=https://your-vercel-app.vercel.app,https://your-domain.com
```

**Как сгенерировать SECRET_KEY:**
```python
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

### Шаг 3: Автоматический деплой

✅ После первого деплоя Railway автоматически будет деплоить при каждом `git push` в ветку `main`!

**Ваш API будет доступен по адресу:** `https://your-project.railway.app`

---

## 📝 Быстрый старт

1. **Закоммитьте и запушьте изменения:**
   ```bash
   git add .
   git commit -m "Setup deployment configs"
   git push origin main
   ```

2. **Подключите Vercel** (см. выше) - фронтенд задеплоится автоматически

3. **Подключите Railway** (см. выше) - бэкенд задеплоится автоматически

4. **Обновите CORS** в Railway, добавив URL вашего Vercel фронтенда

5. **Готово!** При каждом `git push` оба сервиса будут автоматически обновляться!

---

## 🔄 Обновление проекта

Просто делайте `git push` - деплой произойдет автоматически:

```bash
git add .
git commit -m "Your changes"
git push origin main
```

Vercel и Railway автоматически обнаружат изменения и задеплоят новую версию!

